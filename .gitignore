# Dependencies
node_modules/
package-lock.json

# Build output
dist/
build/

# Python backend
backend/__pycache__/
backend/*.pyc
backend/.venv/
backend/venv/
backend/.env
__pycache__/
*.pyc

# Environment files
.env
.env.local
.env.*.local

# Claude Code (dev tooling — local settings stay private, but
# skills/agents committed under .claude/ ship with the repo so the
# whole team gets them).
.claude/*
!.claude/skills/
!.claude/agents/
CLAUDE.md

# Reference materials (copyrighted textbook PDFs — not for public repo)
Instructions/
Project/

# SQLite databases (per-professor problem banks live here at runtime)
backend/educator/bank.db
backend/educator/bank.db-journal

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.tmp-commit-msg
