fastapi>=0.111.0
uvicorn[standard]>=0.29.0
numpy>=1.26.0
pydantic>=2.0.0
# httpx is used directly by /api/admin/agent/draft to forward to
# Anthropic / OpenAI. It's transitively pulled in by FastAPI today,
# but listing it explicitly so a clean Render install definitely has it.
httpx>=0.27.0
# Storage layer (backend/educator/db.py). SQLAlchemy 2.x lets the same
# code talk to either SQLite (local dev) or Postgres (Render +
# Supabase). psycopg2-binary is the Postgres driver — only used when
# DATABASE_URL points at a postgresql:// URL. Local dev with no
# DATABASE_URL doesn't load it.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
